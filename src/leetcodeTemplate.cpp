#include<string>

class Solution {
public:
    string multiply(string num1, string num2) {
        string larger_number;
        string smaller_number;
        num1.length()>=num2.length()?larger_number=num1:larger_number=num2;
        int lastIndex=larger_number.length();
        for(int i=smaller_number.length();i>=0;i--){
            
        }
        
    }
};
